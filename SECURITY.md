# Security policy

Please report vulnerabilities privately through GitHub's security-advisory
feature instead of opening a public issue. Do not include API keys, private
character data, saved worlds, generated media, or authentication cookies in a
report.

Supported development happens on the default branch. Before reporting a
dependency issue, reproduce it with the lockfile and include the output of
`npm audit` with secrets removed.
